import 'package:flutter/material.dart';

class UtilFunctions {
  void logoutCallBack(
      {required NavigatorState navigatorkey,
      String message = 'Unauthorized access: Expired token'}) {
    // navigatorKey.currentState!.pushReplacementNamed(route.login);
  }
}
